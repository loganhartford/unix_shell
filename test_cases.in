# Funcitonal Tests:

/usr/bin/printf "The cat's name is %s.\n" 'Theodore Roosevelt'

/usr/bin/printf "Missing quote

cd /nonexistent

head /nonexistent

   /bin/echo   hello

fakecommand

/bin/echo  "test\0test"

"      "

/bin/echo $'\x01\x02\x03\x04'

:(){ :|:& };:

sh -c "sh -c 'sh -c sh'"

/bin/echo $'\xFF\xFE\xFD'

/bin/echo <&999

(SIGQUIT)

cd "fake_dir_that_is_super_long_with_1000_chars_$(head -c 1000 < /dev/zero)"

# Failed
/bin/cat /dev/zero


# Test cases:

cd /opt 
ls
pwd

/bin/ls -l

/bin/echo extra   spaces     will   be    removed

echo "but not if they're    in   quotes"

mkdir '/tmp/"hello"'
cd '/tmp/"hello"'
pwd
cd ..
pwd

false

/bin/sh -c 'exit 7'


